// Legacy file retained for compatibility. Application now uses router via AppLayout.
export default function App() {
  return <p style={{ padding: '2rem', fontFamily: 'Inter, sans-serif' }}>Router initialized. Use <code>main.jsx</code> with <code>RouterProvider</code>.</p>
}
